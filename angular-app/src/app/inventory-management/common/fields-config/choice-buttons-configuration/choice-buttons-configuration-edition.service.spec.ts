/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { ChoiceButtonsConfigurationEditionService } from './choice-buttons-configuration-edition.service';

describe('Service: ChoiceButtonsConfigurationEdition', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [ChoiceButtonsConfigurationEditionService]
    });
  });

  it('should ...', inject([ChoiceButtonsConfigurationEditionService], (service: ChoiceButtonsConfigurationEditionService) => {
    expect(service).toBeTruthy();
  }));
});
