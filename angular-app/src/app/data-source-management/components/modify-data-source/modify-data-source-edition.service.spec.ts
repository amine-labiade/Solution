/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { ModifyDataSourceEditionService } from './modify-data-source-edition.service';

describe('Service: ModifyDataSourceEdition', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [ModifyDataSourceEditionService]
    });
  });

  it('should ...', inject([ModifyDataSourceEditionService], (service: ModifyDataSourceEditionService) => {
    expect(service).toBeTruthy();
  }));
});
