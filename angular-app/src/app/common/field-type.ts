export enum FieldType {
  dropdown,
  file_input,
  slider,
  radio_buttons,
  checkboxes,
  text_input,
  date_input,
  text_output,
  file_output,
  link_output,
}

export enum InputFieldType {
  dropdown,
  file_input,
  slider,
  radio_buttons,
  checkboxes,
  text_input,
  date_input,
}

export enum OutputFieldType {
  text_output,
  file_output,
  link_output,
}

export enum ChoiceButtonsType {
  radio,
  checkbox,
}
